{
  "title": "Swap Rules Engine SLO",
  "panels": [
    {
      "title": "Enrich latency P99",
      "targets": [{ "expr": "histogram_quantile(0.99, rate(rules_engine_enrich_seconds_bucket[5m]))" }]
    },
    {
      "title": "No-match rate by target",
      "targets": [{ "expr": "rate(rules_no_match_count[5m])" }]
    }
  ]
}
